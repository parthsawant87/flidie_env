# requirements.txt — FLIDIE Environment
# ─────────────────────────────────────────────────────────────────────────────
# Pin exact versions. Floating versions (>=) cause "worked on my machine"
# failures when HF Spaces installs a newer version with breaking changes.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core server stack ────────────────────────────────────────────────────────
fastapi==0.111.0      # Web framework. 0.111 is stable with Pydantic v2.
uvicorn==0.29.0       # ASGI server. Powers the FastAPI app in Docker.
pydantic==2.7.1       # MUST be v2. Files 02-04 use .model_dump(), .model_validate().
                      # Pydantic v1 will crash on these methods.

# ── OpenEnv spec ─────────────────────────────────────────────────────────────
openenv-core>=0.2.0  # OpenEnv client + validator. Check latest before submission:
                      #   pip index versions openenv-core
                      # If a newer version exists, update here AND in pyproject.toml.

# ── Testing (only needed locally, excluded from Docker --no-dev) ─────────────
pytest==8.2.0         # Test runner for test_graders.py
httpx==0.27.0         # Async HTTP client used by FastAPI's TestClient in integration tests

openai==1.30.1
python-dotenv==1.0.1